package com.cloudpulse.adapter;

/** The response shape detected while inspecting an HTTP monitoring target. */
public enum HttpResponseFormat {
    STANDARD_JSON,
    LEGACY_JSON,
    GENERIC_HTTP
}
